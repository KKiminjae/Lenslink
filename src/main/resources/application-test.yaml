spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lenslink
    username: root
    password: test1234

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true