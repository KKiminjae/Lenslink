spring:
  datasource:
    url: jdbc:mysql://localhost:3307/lenslink_test
    username: lenslink_test_user
    password: test_password

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

openai:
  api-key: dummy

naver:
  client-id: dummy
  client-secret: dummy
