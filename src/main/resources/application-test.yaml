spring:
  datasource:
    url: ${TEST_DATABASE_URL:jdbc:mysql://localhost:3307/lenslink_test}
    username: ${TEST_DATABASE_USERNAME:lenslink_test_user}
    password: ${TEST_DATABASE_PASSWORD:test_password}

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

openai:
  api-key: dummy

naver:
  client-id: dummy
  client-secret: dummy
